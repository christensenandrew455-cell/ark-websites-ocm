rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /ocmClients/{clientId}/{sectionKey}/{rowId} {
      allow read, create, update, delete: if
        sectionKey in ['preClients', 'clients', 'postClients'];
    }
  }
}
